PORT=3030
HOST=0.0.0.0
# Max bytes of scrollback buffered per service for late-joining clients
MAX_SCROLLBACK=200000
# Default shell used when a service definition omits `command`
DEFAULT_SHELL=
# Signs session cookies + encrypts stored SSH passwords. Set a long random string.
SESSION_SECRET=change-me-to-a-long-random-string
